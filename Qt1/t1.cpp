#include"Queue.h"
/*int main()
{
	int num, i;
	QElemType s;
	LinkQueue Q;
	InitQueue(Q);
	scanf_s("%d", &num);
	for (i = 1; i <= num; i++) {
		scanf_s("%d", &s);
		EnQueue(Q, s);
	}
	scanf_s("%d", &num);
	for (i = 1; i <= num; i++) {
		DeQueue(Q, s);
	}
	if (QueueEmpty(Q))
		printf("Y\n");
	else {
		printf("N\n");
		GetHead(Q, s);
		printf("%d\n", s);
		QueueTraverse(Q);
	}
	return 0;
}*/